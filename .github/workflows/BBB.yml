# ziye
# github地址  https://github.com/6Svip120apk69
# TG频道地址  https://t.me/ziyescript
# TG交流群    https://t.me/joinchat/AAAAAE7XHm-q1-7Np-tF3g
# boxjs链接   https://raw.githubusercontent.com/6Svip120apk69/gitee_q8qsTAUA_cThxc1RBVUE/main/Task/ziye.boxjs.json



# ⚠️⚠️⚠️特别重要,务必进行替换操作，不得使用原作者地址

# 具体方法请看: https://help.github.com/actions/language-and-framework-guides/using-nodejs-with-github-actions



#  以账号    wuhoushaonian
#  仓库地址  https://github.com/wuhoushaonian/yangquan 
#  文件地址   https://raw.githubusercontent.com/wuhoushaonian/yangquan/main/Task/bububao.js    为例 


# 主要替换的字符👇

# 宝     //js名字
# 17,48 0   //定时，如北京时间8到23点，设置0-15
# BBB_bububaoTOKEN   //环境变量之一，47行处自行添加其他
# wuhoushaonian      //账号名
# yangquan      //仓库名
# main     //分支名
# Task/bububao.js  //子文件地址




name: 宝

on:
  workflow_dispatch:
  schedule:
     - cron: '17,48 0-14,17-23 * * *'
  watch:
    types: started
jobs:
  build:
    runs-on: ubuntu-latest
    if: github.event.repository.owner.id == github.event.sender.id
    env:
        BBB_bububaoTOKEN: ${{ secrets.BBB_bububaoTOKEN }}		
        
    steps:
      - name: Checkout
        run: |
          git clone https://github.com/wuhoushaonian/yangquan.git ~/yangquan
      - name: Use Node.js 12.x
        uses: actions/setup-node@v1
        with:
          node-version: 12.x
      - name: npm install
        if: env.BBB_bububaoTOKEN
        run: |
          cd ~/yangquan
          git checkout main
          npm install
      - name: '运行 【宝】'
        if: env.BBB_bububaoTOKEN
        run: |
          cd ~/yangquan
          node Task/bububao.js
        env:
          
          PUSH_KEY: ${{ secrets.PUSH_KEY }}
          BARK_PUSH: ${{ secrets.BARK_PUSH }}
          TG_BOT_TOKEN: ${{ secrets.TG_BOT_TOKEN }}
          TG_USER_ID: ${{ secrets.TG_USER_ID }}
          BARK_SOUND: ${{ secrets.BARK_SOUND }}
          DD_BOT_TOKEN: ${{ secrets.DD_BOT_TOKEN }}
          DD_BOT_SECRET: ${{ secrets.DD_BOT_SECRET }}
